port: "8080"
dbPath: "quotepro.db"
jwtSecret: "quotepro-secret-key-change-in-production"

ai:
  # Ark（火山方舟）OpenAI-compatible 网关地址（你的需求：coding/v3）
  baseUrl: "https://ark.cn-beijing.volces.com/api/coding/v3"
  # 把你的 key 粘贴到 backend/config.yaml（不要提交到 git）
  apiKey: ""
  # 这里填写 Ark 控制台里对应的 model 字符串（必须精确）
  model: "doubao-seed-2.0-code"

